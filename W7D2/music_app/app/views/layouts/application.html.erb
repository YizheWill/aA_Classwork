<!DOCTYPE html>
<html>
  <head>
    <title>MusicApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <% flash[:errors]&.each do |e| %>
      <p><%= e %></p>
    <% end %>
    <div style="display: flex; justify-content: center;">
      <h1>MUSIC APP</h1>
    </div>
    <div style="display: flex; align-items: center; justify-content: space-around;">
      <%= button_to 'Take me Home', bands_url, method: :get %>
      <% if current_user %>
        <div style="display: flex; align-items: center;">Logged in as: <%= current_user.email %> <%= button_to 'Sign Out', session_url, method: :delete %></div>
      <% else %>
        <%= button_to 'Go to Sign-up!', new_user_url, method: :get %>
      <% end %>
    </div>
    <div style="display: block; margin: 20px auto; max-width: 300px;">
      <div style="background-color: #e2e2e2; display: flex; flex-direction: column; align-items: center; padding-bottom: 20px">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
